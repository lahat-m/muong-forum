module.exports = {
  theme: {
    extend: {
      animation: {
        fade: "fadeIn 3s ease-in-out infinite",
      },
      keyframes: {
        fadeIn: {
          '0%, 100%': { opacity: 0.8 },
          '50%': { opacity: 1 },
        },
      },
    },
  },
}
